import LoadSpinner from "@/components/LoadSpinner";

const Loading = () => {
  return (
    <section>
      <p>chargement de l'application...</p>
      <LoadSpinner />
    </section>
  )
}

export default Loading;
