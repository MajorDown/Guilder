'use client'
import PageForManager from "@/components/manager/PageForManager"

const GestionPage = ():JSX.Element => {
    return (<PageForManager id={""} title={'section de gestion'}>
        <p>page de départ des manager</p>
    </PageForManager>)
}

export default GestionPage